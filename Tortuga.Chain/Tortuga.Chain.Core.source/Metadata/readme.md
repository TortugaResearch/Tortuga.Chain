This namespace is for the shared metadata classes that all datasources theoritically use. 

Application code would only import this namespace when it needs to work directly with the database schema.

